{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://webforms.docyrus.app/schemas/webform-submission-envelope.schema.json",
  "title": "Docyrus Webforms public submission envelope",
  "description": "Static envelope posted by the public webform runtimes. Validate `data` separately against the form-specific `json_schema` generated by webformSchemaToJsonSchema().",
  "type": "object",
  "required": [
    "data"
  ],
  "properties": {
    "data": {
      "type": "object",
      "additionalProperties": true
    },
    "turnstileToken": {
      "type": "string",
      "minLength": 1
    }
  },
  "additionalProperties": false
}
